<div class="gamebg">
  <div class="container">
    <div class="formtile">
      <h1>Games</h1>
      <div>
        <div class="row">
          {{#each games}}
            <div class="col-md-4 mb-5">
              <div class="card" style="width: 20rem;">
                <a href="/game-details/{{_id}}"><img
                    src="{{picture}}"
                    class="card-img-top"
                    alt="{{gameName}}"
                    onerror="this.src='../images/default-user.png'"
                  /></a>
                <div class="card-body">
                  <h5 class="card-title">{{gameName}}</h5>
                  <p class="card-title">Author: {{createdBy.username}}</p>
                  <a href="/users/{{id}}/edit" class="btn btn-primary">Edit</a>
                </div>
              </div>
            </div>
          {{else}}
            <p>There are no games!</p>
          {{/each}}
        </div>
        <p class="page page-menu padding-bottom-md">Page {{page}} of {{pages.length}} pages</p>
        <div class="page-menu">
          {{#each pages}}<a
              class="page"
              href="/game-library/?page={{this}}"
            >Page
              {{this}}</a>{{/each}}
        </div>
      </div>
    </div>
  </div>
</div>